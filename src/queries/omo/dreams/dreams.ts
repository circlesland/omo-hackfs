export class Dreams
{

/*
    dreams:any;
    query() {
        this.dreams = window.o.graphQL.subscribe(
            "Messages{_id,text, name, date, ChatRoom{_id}}",
            result =>
            {
                this.dreams = result;
            }
        );
    }
 */
}
